[project]
name = "simple-llm-web"
version = "0.1.0"
description = "简单的网页用于与LLM聊天"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "openai>=1.68.0",
    "pydantic-settings>=2.6.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
